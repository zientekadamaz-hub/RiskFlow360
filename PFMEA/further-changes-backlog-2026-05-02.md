# Output D - Further changes backlog

Data: 2026-05-02

| ID | Area | Title | Description | Severity | Business impact | Technical risk | Recommended action | Dependencies | Requires migration? | Requires downtime? | Safe for immediate implementation? | Effort | Priority |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| B-001 | Supabase | Canonical migration source | Partially implemented: wykonano lokalna inwentaryzacje migracji, potwierdzono 14/14 duplikatow w `supabase/migrations`, wskazano 27 plikow `db/`-only i dodano deployment checklist. Nadal wymagany schema dump/diff przed pelna konsolidacja. | High | Wysoki: nowe srodowiska moga byc inne niz produkcja. | High | Utrzymac `supabase/migrations/` jako kanoniczne zrodlo nowych migracji; nastepnie wykonac schema dump/diff i sklasyfikowac pliki `db/`-only. | Dostep do live DB/staging. | Yes | No | No | M | P0 |
| B-002 | Security | Rotate exposed secrets | Sekrety kopiowane recznie poza bezpieczne storage powinny byc uznane za ujawnione. | High | Wysoki: ryzyko nieautoryzowanego dostepu. | Medium | Rotacja hasel/kluczy, aktualizacja `.env.local` i hostingu. | Dostep admin Supabase. | No | No | Yes | S | P0 |
| B-003 | Testing | Dedicated regression environment | Skrypty regresyjne istnieja, ale nie ma env i dedicated project. | High | Wysoki: brak pewnosci przy zmianach auth/PFMEA. | Medium | Utworzyc regression org/project, uzupelnic env, odpalac w CI. | Supabase staging/test data. | No | No | Yes | M | P0 |
| B-004 | Security | Distributed request-access rate limit | Process-local rate limit zostal uzupelniony o DB-level duplicate protection dla aktywnych statusow, ale nadal nie ma limitu per IP/provider. | Medium | Sredni: spam access requests z wielu adresow lub spoofowanych danych. | Medium | Dodac provider-level/edge rate limit albo server-side service-role endpoint z trwalym rate-limit store. | Decyzja hostingowa i ewentualny service role env. | Maybe | No | Yes with staged rollout | M | P1 |
| B-005 | Architecture | PFMEA service layer | Partially implemented: czyste kalkulacje PFMEA RPN/oxd/current risk sa w `pfmea-risk-utils.ts`, hierarchia/group ids/merge spans w `pfmea-hierarchy-utils.ts`, reindex/insert/sort/order metadata w `pfmea-row-order-utils.ts`, a row factories, payload/select contracts, normalizacja wartosci, decyzja PCP, matchowanie wierszy, walidacja Action Plan, continuation checks, date helpers, operation/PFD helpers, revision/display helpers i hydratacja group ids w dedykowanych `src/features/pfmea/*-utils.ts`; moduly maja smoke testy. PFMEA page nadal laczy UI, Supabase, drafty i publish. | High | Wysoki: kazda zmiana PFMEA jest droga. | High | Kolejny etap: wydzielic `pfmea-service`, `usePfmeaRevision`, `usePfmeaRows` i operacje draft/publish za testami regresyjnymi. | Testy regresyjne. | Maybe | No | No | L | P1 |
| B-006 | Architecture | AppHeader decomposition | Implemented: decyzje headera, menu model, logo, public actions, nawigacja, dropdown i user controls zostaly wydzielone; dodano smoke regression `regression:app-header`. | Low | Wysoki pozytywny: login/menu/org bugs sa latwiejsze do izolacji. | Low | Utrzymac logike auth/cache/RPC w jednym miejscu; kolejny etap tylko po dodaniu browser smoke dla headera. | Shell smoke tests. | No | No | Done | M | P3 |
| B-007 | UI Standard | Split `invitation-shell` into rf-ui modules | Implemented: standard UI zostal fizycznie podzielony na moduly `tokens`, `forms`, `tables`, `dialogs`, `layout`, `risk-matrix`, `sections`, `summary`, `buttons`, `popovers` i `status`; stary `invitation-shell` pozostaje tylko adapterem kompatybilnosci. | Low | Sredni: nowe strony maja kanoniczne, mniejsze moduly standardu. | Low | Utrzymac importy przez `@/components/rf-ui`; przy kolejnych zmianach nie dodawac nowych stylow do adaptera legacy. | Brak. | No | No | Done | M | P3 |
| B-008 | Reports | Shared RPN/risk color engine | Implemented: PFMEA, Projects, RPN Matrix i Progress Chart korzystaja ze wspolnego `src/lib/risk-engine.ts`; dodano smoke regression. | Low | Wysoki pozytywny: mniejsze ryzyko rozjazdu kolorow miedzy ekranami. | Low | Utrzymac wspolny engine i rozszerzyc test przy kolejnych zmianach Risk Matrix. | Brak. | No | No | Done | S | P3 |
| B-009 | Performance | Verify indexes for reports/PFMEA | Nie potwierdzono indeksow dla wszystkich goracych query. | Medium | Sredni: ryzyko spadku wydajnosci przy wiekszej bazie. | Medium | `EXPLAIN`, `pg_indexes`, dodanie indeksow tylko po pomiarze. | Live/staging DB. | Yes | No | No | M | P1 |
| B-010 | React | Remove legacy ESLint overrides | Partially implemented: usunieto niepotrzebne override `no-explicit-any` dla Projects, Sites & Departments i Risk Matrix oraz usunieto override `ban-ts-comment` dla Risk Matrix. PFMEA/PFD/PCP/invitations/header nadal maja realne debt. | Medium | Sredni: mniej pewnosci React Compiler. | Medium | Modul po module usuwac przy refaktorach; nie wymuszac reguly tam, gdzie generuje realne regresyjne ryzyko. | PFMEA/PFD/PCP/Header refactor. | No | No | Yes | M | P2 |
| B-011 | UX/Error | Domain error mapper | Implemented foundation: `src/lib/error-utils.ts` zawiera teraz `AppError`, klasyfikacje auth/validation/conflict/db/permission/network/timeout i `toUserErrorMessage`; uzyte w Risk Matrix, Projects, Task Management, Customer Access, Sites & Departments, RPN Matrix i Progress Chart. | Low | Sredni pozytywny: mniej rozjazdow w technicznych komunikatach. | Low | Przy kolejnych refaktorach przepinac pozostale legacy strony i dopisywac domenowe copy per flow. | Service layer. | No | No | Done foundation | M | P3 |
| B-012 | Accessibility | Full keyboard/focus audit | Partially implemented: globalny header obsluguje Enter/Space dla menu, dropdown links maja `role=menuitem`, a standardowe confirm dialogi maja `role=dialog`, `aria-modal`, `aria-labelledby` i Escape close. Pelny audyt tabel/wykresow nadal otwarty. | Medium | Sredni: dostepnosc i profesjonalizm UI. | Low | Kolejny etap: focus trap w modalach, aria labels dla wykresow, keyboard path dla tabel i custom selectow. | Stabilny rf-ui. | No | No | Yes | M | P2 |
| B-013 | Supabase | Transactional PFMEA publish/draft RPC | Partially implemented and deployed: dodano oraz wdrozono `publish_pfmea_revision_with_history(...)`; frontend ma fallback. Nadal do zrobienia: w kolejnym etapie przeniesc row metadata/order persist do kontrolowanego RPC, jesli timer pokaze dalsze bottlenecki. | High | Wysoki: mniejsze ryzyko czesciowego zapisu i krotszy save path. | Medium | Sprawdzic `PFMEA save timings` na problematycznym projekcie. Jesli zapis nadal trwa zbyt dlugo, zaprojektowac drugi RPC dla row metadata/order persist. | Test zapisu PFMEA na realnym projekcie. | Yes | No | Yes with staged fallback | M | P1 |
| B-014 | CI/CD | Add full `npm run check` to CI | Implemented: lokalne `npm run check` zawiera `lint`, `typecheck`, `regression:shared` i `build`; GitHub Actions uruchamia `regression:shared` w jobie build bez sekretow. | Low | Sredni pozytywny: mniej regresji w GitHub. | Low | Utrzymac browser regression jako opcjonalny job zależny od sekretow; dodawac kolejne smoke testy do `regression:shared`. | Brak. | No | No | Done | S | P3 |
| B-015 | Strict mode | Re-enable React strict mode | `reactStrictMode` jest wylaczony. | Medium | Sredni: ukryte problemy efektow. | Medium | Wlaczyc po usunieciu najwiekszego hook debt i smoke testach. | B-010. | No | No | No | S | P3 |
| B-016 | Seed scripts | Production guard for seed script | Implemented: seed demo wymaga teraz `ALLOW_DEMO_SEED=YES`; pozostaje uzywac go tylko na znanej organizacji demo/staging. | Low | Niski po zmianie: przypadkowe uruchomienie jest blokowane. | Low | Utrzymac guard i rozwazyc osobny staging seed profile. | Brak. | No | No | Done | S | P3 |
| B-017 | Supabase Security | Move sensitive RPC behind server/service role | Pozostale `SECURITY DEFINER` RPC sa celowo uzywane jako granica uprawnien, ale nadal widoczne w PostgREST dla roli `authenticated` lub `anon`. | Medium | Sredni: mniejsza powierzchnia API ulatwi audyty enterprise. | Medium | Dla admin/customer-access/invitation operations dodac server routes z service role, potem odebrac bezposredni `EXECUTE` zwyklym rolom. | Service role env, staging, regresje auth/invitations. | Yes | No | No | L | P1 |
| B-018 | Supabase Auth | Enable leaked password protection | Supabase advisors nadal raportuje wylaczona ochrone przed haslami z wyciekow. | Medium | Sredni: slabsze hardening auth dla nowych kont. | Low | Wlaczyc w Supabase Dashboard/Auth settings, jesli plan projektu to obsluguje. | Dostep admin Supabase. | No | No | Yes | S | P1 |
| B-019 | Supabase Performance | Deploy PFMEA save timeout indexes | Implemented and deployed: `supabase/migrations/20260502113000_pfmea_save_timeout_indexes.sql` zostala wykonana na linked live project i zweryfikowano istnienie indeksow `idx_pfmea_rows_operation_id`, `idx_pfmea_rows_revision_operation`, `idx_pfmea_rows_revision_created_id`, `idx_operations_project_active_number`. | Low | Wysoki pozytywny: mniejsze ryzyko timeoutow zapisu PFMEA na wiekszych rewizjach. | Low | Monitorowac zapis PFMEA na projekcie, ktory raportowal timeout; jesli problem wroci, przeniesc publish/draft do transakcyjnego RPC. | Brak. | Done | No | Done | S | P3 |
| B-020 | Observability | PFMEA save timing review | Implemented foundation: aplikacja zapisuje etapowe timingi PFMEA save do konsoli i `window.__RF360_LAST_PFMEA_SAVE_TIMINGS`. | Medium | Sredni: pozwala wskazac realne bottlenecki zamiast zgadywania. | Low | Po kolejnym wolnym zapisie skopiowac liste timingow i zdecydowac, czy bottleneck jest w draft row update, row order, publish RPC, history czy refresh. | Uzytkownik lub QA musi wykonac zapis na realnym projekcie. | No | No | Yes | S | P1 |
| B-021 | Architecture | PCP service layer | Partially implemented: helpery PCP przeniesione do `src/features/pcp/pcp-utils.ts` i objete smoke testem. Strona PCP nadal laczy UI, Supabase, edit session, seed z PFMEA i publish. | Medium | Sredni: zmiany PCP beda czestsze po stabilizacji PFMEA/PCP flow. | Medium | Kolejny etap: wydzielic `pcp-service`, publish/history helper i hook stanu tabeli. | Testy regresyjne dla PCP save. | Maybe | No | Yes, incremental | M | P2 |
